{
    fn function() -> i64 {
        let x = 1;
        return x;
        print(3); // Warning: dead code.
    };

    function(); // Warning: return value discarded.

    for i in [1, 4] {
        for i in [1, 4] { // Warning: shadowing variable with the same name and type.
            print(i);
        };
    };

    print(<u32>(-1)); // Warning: convertion loss.
}