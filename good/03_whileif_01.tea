{
    let x = 6;
    if (x < 0) {
        print("No.");
    };

    while x > 0 {
        if x % 2 == 1 {
            print(1);
        } else {
            print(0);
        };

        x = x / 2;
    };
}
