{
    fn factorial(n : u64) -> u64 {
        if n == <u64>(0) {
            return <u64>(1);
        };
        
        return factorial(n - <u64>(1)) * n;
    };

    fn procedure() -> void {
        print("This is a procedure!");
    };

    print(factorial(<u64>(6)));
    procedure();
}
