{
    fn f() -> bool {
        if 4 < 3 {
            return false;
        } else if 2 < 3 {
            let x = 2;
        } else {
            return true;
        };
        // No return statement
    };
}
